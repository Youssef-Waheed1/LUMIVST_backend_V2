# 📝 تعليمات تحديث بيانات الأدمن

## الطريقة الأولى: تحديث تفاعلي (Interactive)

```bash
cd backend
venv\Scripts\python update_admin.py
```

سيطلب منك البرنامج:
1. الإيميل الجديد (أو اضغط Enter للإبقاء على القديم)
2. الباسورد الجديد (أو اضغط Enter للإبقاء على القديم)
3. تأكيد التغييرات

---

## الطريقة الثانية: تحديث سريع (Quick)

```bash
cd backend
venv\Scripts\python quick_update_admin.py <new_email> <new_password>
```

**مثال:**
```bash
venv\Scripts\python quick_update_admin.py myemail@example.com MySecurePass123
```

---

## البيانات الحالية

```
Email: admin@lumivst.com
Password: adminpassword123
```

---

## ملاحظات مهمة

⚠️ **احفظ البيانات الجديدة في مكان آمن!**

⚠️ **بعد التحديث، استخدم البيانات الجديدة لتسجيل الدخول**

✅ **التحديث يتم مباشرة في قاعدة البيانات**

---

## إصلاح مشكلة Contact Form

تم إصلاح المشكلة! الآن الـ Contact Form يرسل الرسائل مباشرة إلى الـ Backend API.

**التغييرات:**
- ✅ تم تحديث `/app/contact/page.tsx` ليرسل إلى `http://localhost:8000/api/contact/`
- ✅ الرسائل الآن ستظهر في الـ Admin Dashboard مباشرة

**للاختبار:**
1. اذهب إلى `http://localhost:3000/contact`
2. املأ النموذج وأرسل رسالة
3. سجل دخول في `http://localhost:3000/admin/login`
4. ستجد الرسالة في الـ Dashboard!
